% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/muscle-tendon.R
\name{defaultMusclePath}
\alias{defaultMusclePath}
\title{Build a linear muscle-path model from the moment-arm lookup}
\usage{
defaultMusclePath(muscle, joint = NULL, slack_length = 0.3, hill = NULL)
}
\arguments{
\item{muscle}{Muscle name (as in \code{\link[=momentArm]{momentArm()}}).}

\item{joint}{Optional joint name (required only if the muscle appears for
more than one joint).}

\item{slack_length}{MTU slack length (m) offset (default 0.3).}

\item{hill}{Optional Hill parameters (see \code{\link[=musclePath]{musclePath()}}).}
}
\value{
A \code{muscle_path} object with a constant moment arm.
}
\description{
Constructs a \code{\link[=musclePath]{musclePath()}} with a constant (angle-independent) moment arm
taken from the anatomical lookup table, i.e. a linear MTU length
\code{L(theta) = slack_length - r * theta}. Useful as a default path when no
subject-specific polynomial model is available.
}
\examples{
defaultMusclePath("Soleus")
}
\seealso{
\code{\link[=musclePath]{musclePath()}}, \code{\link[=muscleTendonKinematics]{muscleTendonKinematics()}}
}
