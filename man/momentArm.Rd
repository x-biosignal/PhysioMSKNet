% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/muscle-tendon.R
\name{momentArm}
\alias{momentArm}
\title{Muscle moment arms}
\usage{
momentArm(muscle = NULL, joint = NULL, model = NULL, angles = NULL)
}
\arguments{
\item{muscle}{Optional muscle name to filter the lookup table (case
insensitive). Ignored when \code{model} is supplied.}

\item{joint}{Optional joint name to filter the lookup table.}

\item{model}{Optional \code{\link[=musclePath]{musclePath()}} model; when supplied, moment arms are
computed from the path derivative at \code{angles}.}

\item{angles}{Named numeric vector of joint angles in radians (one per joint
the \code{model} crosses); required when \code{model} is supplied.}
}
\value{
Without \code{model}: a data frame with \code{muscle_name}, \code{joint_name},
\code{moment_arm_m} (magnitude), \code{direction}, and \code{signed_moment_arm_m}. With
\code{model}: a named numeric vector of signed moment arms (m) per joint.
}
\description{
Returns muscle moment arms. With no \code{model}, this is the anatomical lookup
table promoted from the package's internal reference values (signed by the
agonist/antagonist direction); with a \code{\link[=musclePath]{musclePath()}} \code{model}, the moment arm
is the joint-angle-dependent value \code{-dL/dtheta} evaluated at \code{angles}.
}
\examples{
momentArm("Gastrocnemius")
mp <- musclePath("ankle", coefficients = list(ankle = c(-0.05)))
momentArm(model = mp, angles = c(ankle = 0.2))
}
\references{
Sherman MA, Seth A, Delp SL (2013). "What is a moment arm? Calculating muscle
effectiveness in biomechanical models using generalized coordinates."
}
\seealso{
\code{\link[=musclePath]{musclePath()}}, \code{\link[=muscleTendonKinematics]{muscleTendonKinematics()}}
}
