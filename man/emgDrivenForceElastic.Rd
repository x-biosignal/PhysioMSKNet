% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hill-elastic.R
\name{emgDrivenForceElastic}
\alias{emgDrivenForceElastic}
\title{EMG-driven force with a compliant tendon}
\usage{
emgDrivenForceElastic(
  emg,
  mtu_length,
  sr,
  optimal_fiber_length,
  tendon_slack_length,
  params = emgDrivenParams()
)
}
\arguments{
\item{emg}{EMG envelope.}

\item{mtu_length}{MTU length (m); scalar or same length as \code{emg}.}

\item{sr}{Sampling rate (Hz).}

\item{optimal_fiber_length, tendon_slack_length}{Muscle geometry (m).}

\item{params}{Parameter list from \code{\link[=emgDrivenParams]{emgDrivenParams()}}.}
}
\value{
Muscle-tendon force time series (N).
}
\description{
Forward pipeline as \code{\link[=emgDrivenForce]{emgDrivenForce()}} but with tendon compliance: at each
time the muscle-tendon force equilibrium is solved for the fiber length
(quasi-static), so tendon stretch is accounted for. Requires the MTU length
trajectory (e.g. from \code{\link[=muscleTendonKinematics]{muscleTendonKinematics()}}).
}
\examples{
sr <- 500; t <- seq(0, 1, by = 1 / sr)
emg <- pmax(sin(2 * pi * 2 * t), 0)
f <- emgDrivenForceElastic(emg, mtu_length = 0.30, sr = sr,
                           optimal_fiber_length = 0.10,
                           tendon_slack_length = 0.19)
max(f)
}
