% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hill-elastic.R
\name{equilibriumFiberLength}
\alias{equilibriumFiberLength}
\title{Equilibrium fiber length with a compliant tendon}
\usage{
equilibriumFiberLength(
  activation,
  mtu_length,
  optimal_fiber_length,
  tendon_slack_length,
  max_isometric_force = 1000,
  pennation = 0,
  e0t = 0.04
)
}
\arguments{
\item{activation}{Activation in \verb{[0, 1]}.}

\item{mtu_length}{Muscle-tendon-unit length (m).}

\item{optimal_fiber_length}{Optimal fiber length (m).}

\item{tendon_slack_length}{Tendon slack length (m).}

\item{max_isometric_force}{Peak isometric force (N).}

\item{pennation}{Pennation angle (rad).}

\item{e0t}{Tendon reference strain (default 0.04).}
}
\value{
A list with \code{fiber_length}, \code{tendon_length}, \code{norm_fiber_length},
\code{norm_tendon_length}, and \code{force} (N).
}
\description{
Solves the quasi-static (isometric) muscle-tendon force balance for the fiber
length: the fiber active+passive force along the tendon equals the tendon
force, with \verb{tendon length = MTU length - fiber length * cos(pennation)}.
}
\examples{
equilibriumFiberLength(0.5, mtu_length = 0.30,
                       optimal_fiber_length = 0.10,
                       tendon_slack_length = 0.20)
}
