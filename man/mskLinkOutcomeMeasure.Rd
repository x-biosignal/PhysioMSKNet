% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge-outcome.R
\name{mskLinkOutcomeMeasure}
\alias{mskLinkOutcomeMeasure}
\title{Link a network-derived outcome prediction to a clinical outcome measure}
\usage{
mskLinkOutcomeMeasure(
  outcome,
  instrument_id,
  measure = c("function", "rom", "strength"),
  hub = NULL
)
}
\arguments{
\item{outcome}{An \code{MSKFunctionalOutcome} (from
\code{\link[=mskPredictFunctionalOutcome]{mskPredictFunctionalOutcome()}}) or a single numeric prediction.}

\item{instrument_id}{A clinical outcome-measure instrument id (e.g.
\code{"fma_ue"}, \code{"berg"}).}

\item{measure}{Which aggregate prediction to link when \code{outcome} is an
\code{MSKFunctionalOutcome}: \code{"function"} (default), \code{"rom"}, or
\code{"strength"}.}

\item{hub}{Optional \code{PhysioAnnotationHub} passed to
\code{tagICF()}.}
}
\value{
A data.frame with \code{instrument_id}, \code{measure},
\code{predicted_value}, and \code{icf_code} (one row per ICF tag; a single
\code{NA} \code{icf_code} when no ICF link is available).
}
\description{
Maps an MSK-network functional-outcome prediction to a validated clinical
outcome measure (COM) instrument and its WHO ICF category tags (via
\code{PhysioAnnotationHub::tagICF()}, when that package is installed). This
bridges an abstract network prediction to a documented, ICF-anchored clinical
instrument.
}
\examples{
\dontrun{
out <- mskPredictFunctionalOutcome(c("Deltoid", "Biceps Brachii"))
mskLinkOutcomeMeasure(out, "fma_ue", measure = "strength")
}
}
\seealso{
\code{\link[=mskPredictFunctionalOutcome]{mskPredictFunctionalOutcome()}}
}
