% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/muscle-tendon.R
\name{muscleTendonKinematics}
\alias{muscleTendonKinematics}
\title{Muscle-tendon length, velocity and moment arms from joint angles}
\usage{
muscleTendonKinematics(
  model,
  angles,
  angular_velocities = NULL,
  sampling_rate = NULL
)
}
\arguments{
\item{model}{A \code{\link[=musclePath]{musclePath()}} model.}

\item{angles}{Joint angles in radians: a named numeric vector (single pose),
or an \verb{n_frames x n_joints} matrix / data frame / named list of curves with
columns/names matching the model's joints.}

\item{angular_velocities}{Joint angular velocities (rad/s), same shape as
\code{angles}. If \code{NULL} for a trajectory, they are estimated from \code{angles} by
central differences (requires \code{sampling_rate}); for a single pose they
default to zero.}

\item{sampling_rate}{Sampling rate (Hz) used to differentiate \code{angles} when
\code{angular_velocities} is \code{NULL}.}
}
\value{
A \code{muscle_tendon_kinematics} object: a list with \code{length} (m),
\code{velocity} (m/s), \code{moment_arms} (\verb{n_frames x n_joints}, m), \code{joints}, and,
when \code{hill} is set, \code{norm_fiber_length} and \code{norm_fiber_velocity}.
}
\description{
Evaluates a \code{\link[=musclePath]{musclePath()}} over one or more poses: MTU length \code{L(theta)},
moment arms \code{-dL/dtheta_j}, and MTU lengthening velocity
\verb{dL/dt = -sum_j r_j * omega_j}. When the model carries \code{hill} parameters,
rigid-tendon normalized fiber length and velocity are also returned.
}
\examples{
mp <- musclePath("ankle", coefficients = list(ankle = c(-0.05, 0.01)),
                 slack_length = 0.3)
muscleTendonKinematics(mp, angles = c(ankle = 0.1),
                       angular_velocities = c(ankle = 2))
}
\references{
Zajac FE (1989). "Muscle and tendon: properties, models, scaling, and
application to biomechanics and motor control." Thelen DG (2003).
}
\seealso{
\code{\link[=musclePath]{musclePath()}}, \code{\link[=momentArm]{momentArm()}}
}
