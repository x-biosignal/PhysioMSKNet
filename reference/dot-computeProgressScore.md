# Compute weighted progress score

Compute weighted progress score

## Usage

``` r
.computeProgressScore(current, previous)
```

## Arguments

- current:

  Named numeric vector of current metric values.

- previous:

  Named numeric vector of previous metric values.

## Value

Numeric scalar progress score (0-100).
